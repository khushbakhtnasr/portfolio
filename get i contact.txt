<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Contact Section - Movable</title>
<style>
:root{
  --bg:#0f0f10;
  --muted:#bfc3c8;
  --accent:#e74c3c;
  --title:#ffffff;
  font-family: "Inter", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
}

body {
  margin:0;
  background:var(--bg);
  color:var(--muted);
}

/* Contact Section */
.contact-section {
  position: relative;
  width: 100%;
  height: 600px; /* adjust as needed */
  background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(0,0,0,0));
  overflow: hidden;
  border-top: 1px solid rgba(255,255,255,0.03);
}

/* Title */
.contact-title {
  position: absolute;
  top: 70px;  /* move vertically */
  left: 80px; /* move horizontally */
  font-size: 48px;
  font-weight: 700;
  color: var(--title);
}

/* Lead paragraph */
.contact-lead {
  position: absolute;
  top: 250px;
  left: 80px;
  width: 400px; /* control width */
  font-size: 24px;
  line-height: 1.7;
}

/* Follow Column */
.follow-column {
  position: absolute;
  top: 300px;
  left: 50px;
}

.follow-column h3 {
  font-size: 18px;
  color: #fff;
  margin-bottom: 12px;
}

.social-list {
  list-style: none;
  padding: 0;
  margin: 0;
}

.social-list li {
  margin-bottom: 10px;
}

.social-list a {
  color: var(--muted);
  text-decoration: none;
  font-size: 15px;
  transition: color 0.15s;
}

.social-list a:hover {
  color: #fff;
}

/* Contact Column */
.contact-column {
  position: absolute;
  top: 300px;
  right: 50px;
}

.contact-column h3 {
  font-size: 18px;
  color: #fff;
  margin-bottom: 12px;
}

.contact-column a {
  color: var(--muted);
  text-decoration: none;
  display: block;
  margin-bottom: 10px;
  font-size: 15px;
}

.contact-column a:hover {
  color: #fff;
}

/* CV Button */
.cv-btn {
  position: absolute;
  top: 500px;
  right: 50px;
  padding: 10px 14px;
  border: 1px solid rgba(255,255,255,0.06);
  border-radius: 8px;
  background: transparent;
  color: var(--muted);
  font-weight: 600;
  cursor: pointer;
  letter-spacing: 2px;
}

.cv-btn:hover {
  border-color: rgba(255,255,255,0.12);
  color: #fff;
}

/* Fixed Message Tab */
.message-tab {
  position: fixed;
  top: 50%;   /* move vertically */
  left: 0;    /* move horizontally */
  transform: translateY(-50%) rotate(-90deg);
  background: var(--accent);
  color: #fff;
  padding: 10px 18px;
  font-weight: 700;
  letter-spacing: 2px;
  text-decoration: none;
  border-radius: 4px 4px 0 0;
  box-shadow: 0 6px 20px rgba(0,0,0,0.45);
}
</style>
</head>
<body>

<section class="contact-section">

  <!-- Title -->
  <div class="contact-title">Get In Touch.</div>

  <!-- Lead paragraph -->
  <div class="contact-lead">
    Lorem ipsum dolor sit amet consectetur adipisicing elit. Voluptas illum quasi facere libero, fugiat laboriosam possimus amet consectetur adipisicing elit reprehenderit eveniet tempore quisquam ipsa id esse.
  </div>

  <!-- Follow Me Column -->
  <div class="follow-column">
    <h3>Follow Me</h3>
    <ul class="social-list">
      <li><a href="#">Facebook</a></li>
      <li><a href="#">Twitter</a></li>
      <li><a href="#">Instagram</a></li>
      <li><a href="#">Dribbble</a></li>
      <li><a href="#">Hostinger</a></li>
    </ul>
  </div>

  <!-- Contact Column -->
  <div class="contact-column">
    <h3>Contact Me</h3>
    <a href="mailto:youremail@example.com">sayhello@hudson.com</a>
    <a href="tel:+19754323345">+197 543 23345</a>
  </div>

  <!-- CV Button -->
  <button class="cv-btn">GET MY CV</button>

  <!-- Fixed Message Tab -->
  <a href="#contact" class="message-tab">MESSAGE ME</a>

</section>

</body>
</html>
